{
  "manifest_version": 3,
  "name": "ChatGPT Publish to Wordpress",
  "version": "1.0",
  "description": "The ChatGPT Publish to WordPress Chrome Extension allows users to extract the latest message from ChatGPT sessions and publish it as a draft to a WordPress site. Users can configure their API URL and credentials through the extension's settings page.",
  "permissions": ["activeTab","scripting","storage",
    "webRequest",
    "webRequestBlocking",
    "host_permissions"
  ],

  "background": {
    "service_worker": "background.js" 
  },
  "action": {
    "default_popup": "popup.html"
  },
  "options_page": "settings.html" 
}
